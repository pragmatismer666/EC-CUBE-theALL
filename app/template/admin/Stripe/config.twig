{% extends '@admin/default_frame.twig' %}

{% set menus = ['stripe', 'stripe_config'] %}

{% block title %}{{ 'Stripe 管理'|trans }}{% endblock %}
{% block sub_title %}{{ '設定'|trans }}{% endblock %}

{% form_theme form '@admin/Form/bootstrap_4_horizontal_layout.html.twig' %}

{% block stylesheet %}

{% endblock stylesheet %}

{% block main %}
    <form role="form" method="post">
        {{ form_widget(form._token) }}
        <div class="c-contentsArea__cols">
            <div class="c-contentsArea__primaryCol">
                <div class="c-primaryCol">
                    <div class="card rounded border-0 mb-4">
                        <div class="card-header"><span>{{ 'Stripe設定'|trans }}</span></div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-3"><span>{{ 'malldevel.admin.stripe.config.publishable_key'|trans }}</span><span
                                            class="badge badge-primary ml-1">{{ 'malldevel.admin.stripe.config.required'|trans }}</span></div>
                                <div class="col mb-2">
                                    {{ form_widget(form.publishable_key) }}
                                    {{ form_errors(form.publishable_key) }}
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-3"><span>{{ 'malldevel.admin.stripe.config.secret_key'|trans }}</span><span
                                            class="badge badge-primary ml-1">{{ 'malldevel.admin.stripe.config.required'|trans }}</span></div>
                                <div class="col mb-2">
                                    {{ form_widget(form.secret_key) }}
                                    {{ form_errors(form.secret_key) }}
                                </div>
                            </div>
{#                            <div class="row">#}
{#                                <div class="col-3"><span>{{ 'malldevel.admin.stripe.signature'|trans }}</span></div>#}
{#                                <div class="col mb-2">#}
{#                                    {{ form_widget(form.signature) }}#}
{#                                    {{ form_errors(form.signature) }}#}
{#                                </div>#}
{#                            </div>#}
                            <div class="row">
                                <div class="col-3"><span>{{ 'malldevel.admin.stripe.config.charge_method'|trans }}</span></div>
                                <div class="col mb-2">
                                    {{ form_widget(form.is_capture_on) }}
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-3"><span>{{ 'malldevel.admin.stripe.config.stripe_fees_percent'|trans }}</span></div>
                                <div class="col mb-2">
                                    {{ form_widget(form.stripe_fees_percent) }}
                                    {{ form_errors(form.stripe_fees_percent) }}
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-3"><span>{{ 'malldevel.admin.stripe.config.application_fees_percent'|trans }}</span></div>
                                <div class="col mb-2">
                                    {{ form_widget(form.application_fees_percent) }}
                                    {{ form_errors(form.application_fees_percent) }}
                                </div>
                            </div>
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="c-conversionArea">
            <div class="c-conversionArea__container">
                <div class="row justify-content-between align-items-center">
                    <div class="col-6">
                        <div class="c-conversionArea__leftBlockItem">
                            <a class="c-beseLink"
                            href="{{ url('admin_store_plugin') }}">
                                <i class="fa fa-backward" aria-hidden="true"></i>
                                <span>{{ 'malldevel.admin.stripe.config.back'|trans }}</span>
                            </a>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="row align-items-center justify-content-end">
                            <div class="col-auto">
                                <button class="btn btn-ec-conversion px-5"
                                        type="submit">{{ 'malldevel.admin.stripe.config.save'|trans }}</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
{% endblock %}